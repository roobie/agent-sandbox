---
phase: 04-observability-and-distribution
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/policy/schema.md
  - images/agents/copilot/Dockerfile
autonomous: true
requirements:
  - DIST-03

must_haves:
  truths:
    - "No file outside .planning/ references init-firewall or policy.json as a current mechanism"
    - "README.md describes the Squid proxy approach, not the iptables approach"
    - "Copilot Dockerfile does not COPY a policy.json that does not exist"
    - "docs/policy/schema.md describes the Squid allowlist approach or is removed"
  artifacts:
    - path: "README.md"
      provides: "Updated project description reflecting proxy-based architecture"
      contains: "Squid proxy"
    - path: "images/agents/copilot/Dockerfile"
      provides: "Copilot Dockerfile without stale policy.json reference"
  key_links:
    - from: "README.md"
      to: "config/allowlist.txt"
      via: "documentation reference"
      pattern: "allowlist"
---

<objective>
Clean up orphaned references to the pre-proxy iptables era across the codebase. Update README.md to reflect the current Squid proxy architecture. Remove stale policy.json COPY from copilot Dockerfile. Update or remove docs/policy/schema.md.

Purpose: No user or contributor encounters outdated documentation describing removed functionality.
Output: Updated README.md, cleaned copilot Dockerfile, updated/removed docs/policy/schema.md.
</objective>

<execution_context>
@/home/jani/.claude/get-shit-done/workflows/execute-plan.md
@/home/jani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-observability-and-distribution/04-CONTEXT.md
@.planning/phases/04-observability-and-distribution/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove stale policy.json reference from copilot Dockerfile</name>
  <files>images/agents/copilot/Dockerfile</files>
  <read_first>
    - images/agents/copilot/Dockerfile (current state -- has COPY policy.json that references a file that does not exist on disk)
    - images/agents/claude/Dockerfile (reference for how the claude agent Dockerfile handles this -- likely no policy.json reference)
  </read_first>
  <action>
Remove lines 21-22 from `images/agents/copilot/Dockerfile`:

```dockerfile
COPY policy.json /etc/agent-sandbox/policy.json
RUN chmod 644 /etc/agent-sandbox/policy.json
```

Also remove the comment on line 7-8 that says "Override base policy with Copilot endpoints" since there is no longer a base policy to override. Replace with a comment that reflects the current architecture:

Change:
```dockerfile
# Override base policy with Copilot endpoints
# https://docs.github.com/en/copilot/reference/copilot-allowlist-reference
USER root
```

To:
```dockerfile
# Install mise and Copilot tools as root
USER root
```

The copilot Dockerfile should NOT reference policy.json since egress control is now handled by the Squid proxy allowlist at `config/allowlist.txt`.
  </action>
  <verify>
    <automated>! grep -q 'policy.json' images/agents/copilot/Dockerfile && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - images/agents/copilot/Dockerfile does NOT contain the string "policy.json"
    - images/agents/copilot/Dockerfile does NOT contain "COPY policy.json"
    - images/agents/copilot/Dockerfile does NOT contain "Override base policy"
    - images/agents/copilot/Dockerfile still contains `USER root` (needed for apt install)
    - images/agents/copilot/Dockerfile still contains the mise install block
  </acceptance_criteria>
  <done>Copilot Dockerfile no longer references non-existent policy.json file</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md and docs/policy/schema.md for proxy-based architecture</name>
  <files>README.md, docs/policy/schema.md</files>
  <read_first>
    - README.md (current state -- describes iptables approach throughout)
    - docs/policy/schema.md (current state -- describes init-firewall.py reading policy.json)
    - config/allowlist.txt (current allowlist for reference)
    - images/proxy/squid.conf (current proxy config for reference)
    - mise.toml (current task names for documentation)
    - docker-compose.yml (current compose structure for documentation)
  </read_first>
  <action>
**README.md**: Rewrite sections to reflect current Squid proxy architecture. Key changes:

1. **Line 3 header bullets** -- change "Restricted outbound network (iptables-based allowlist)" to "Restricted outbound network (Squid proxy with domain allowlist via SNI peek/splice)"

2. **"What it does" section (lines 13-18)** -- replace iptables description:
   - Remove "Blocks all outbound network traffic by default"
   - Remove "Allows only specific domains that you specify in a policy file"
   - Remove "Runs as non-root user with limited sudo for firewall initialization in entrypoint"
   - Replace with proxy-based description:
     - Routes all outbound traffic through a shared Squid proxy
     - Allows only domains listed in `config/allowlist.txt` (HTTPS verified via SNI peek/splice, no TLS decryption)
     - Runs as non-root user with all capabilities dropped and read-only rootfs

3. **"Runtime modes" section (lines 20-33)** -- replace the two-mode description. The project now uses mise tasks for lifecycle management:
   - Remove devcontainer/compose mode comparison table
   - Replace with mise-based workflow: `mise run proxy:start`, `mise run sandbox:run`, `mise run sandbox:stop`

4. **"Quick start" section (lines 35-111)** -- rewrite for mise-based workflow:
   - Prerequisites: docker, docker-compose, mise
   - Clone repo
   - `mise run proxy:start` to start proxy
   - `mise run sandbox:run --workspace /path/to/project` to start sandbox
   - `docker exec -it agent-sandbox-default zsh` to enter
   - `mise run sandbox:stop` to stop (shows diffs)
   - Remove devcontainer Option A entirely
   - Remove the old compose Option B

5. **"Network policy" section (lines 113-161)** -- rewrite for allowlist.txt:
   - Domain allowlist is in `config/allowlist.txt`
   - One domain per line, leading dot for subdomain wildcards (e.g., `.github.com`)
   - Override by editing `config/allowlist.txt` and restarting proxy
   - Remove all references to policy.yaml, policy.json
   - Remove the devcontainer.json and docker-compose.yml mount examples for policy files

6. **"How it works" section (lines 163-178)** -- replace iptables description:
   - Sandbox containers join an internal Docker network with no direct internet access
   - A shared Squid proxy bridges to the external network
   - HTTP_PROXY/HTTPS_PROXY env vars route all traffic through Squid
   - Squid uses SNI peek/splice to verify HTTPS destination hostnames without TLS decryption
   - Domains not in allowlist.txt get a TCP_DENIED response
   - Remove all references to init-firewall.sh, iptables, ipset

7. **"Security notes" section (lines 182-190)** -- update:
   - Remove "Firewall verification runs at every container start"
   - Remove references to sudo for firewall setup
   - Add: "All capabilities dropped (--cap-drop=ALL), read-only rootfs, no-new-privileges"
   - Add: "Egress enforced at network layer via Squid proxy, not by trusting the agent"

8. **"Roadmap" section (lines 192-223)** -- simplify:
   - Replace m1-m5 milestones with brief note that the project uses a phased development approach
   - Or remove entirely and point to .planning/ROADMAP.md

**docs/policy/schema.md**: This file documents the old policy.json format which no longer exists. Delete this file entirely -- the Squid allowlist format (one domain per line in allowlist.txt) is simple enough to document in README.md. Run `rm docs/policy/schema.md`.

If `docs/policy/` directory becomes empty after deletion, remove it too: `rmdir docs/policy/ 2>/dev/null`.
  </action>
  <verify>
    <automated>! grep -q 'init-firewall' README.md && ! grep -q 'iptables' README.md && ! grep -q 'policy\.json' README.md && grep -q 'Squid' README.md && grep -q 'allowlist' README.md && grep -q 'mise run' README.md && ! test -f docs/policy/schema.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - README.md does NOT contain "init-firewall" anywhere
    - README.md does NOT contain "iptables" anywhere
    - README.md does NOT contain "policy.json" or "policy.yaml" anywhere
    - README.md contains "Squid" (proxy description)
    - README.md contains "allowlist" (domain allowlist reference)
    - README.md contains "mise run" (lifecycle commands)
    - README.md contains "SNI" (describing peek/splice HTTPS verification)
    - README.md contains "cap-drop" or "capabilities dropped" (hardening description)
    - README.md contains "read-only" (rootfs description)
    - docs/policy/schema.md does NOT exist
    - docs/policy/ directory does NOT exist (or is empty)
  </acceptance_criteria>
  <done>README.md accurately describes the current Squid proxy architecture with mise-based workflow. docs/policy/schema.md removed. No stale iptables-era references remain in non-planning files outside .planning/.</done>
</task>

</tasks>

<verification>
Comprehensive stale reference check:
- `grep -r 'init-firewall\|policy\.json' --include='*.md' --include='*.py' --include='*.yml' --include='*.yaml' --include='*.toml' --include='*.sh' --include='*.json' . | grep -v '.planning/' | grep -v '.git/'` returns only the copilot Dockerfile allowlist comment (if any) or nothing
- `! test -f docs/policy/schema.md` confirms schema.md removed
- `! grep -q 'policy.json' images/agents/copilot/Dockerfile` confirms copilot cleanup
</verification>

<success_criteria>
- No production file (outside .planning/) references init-firewall.py or policy.json as a current mechanism
- README.md describes the Squid proxy approach accurately
- Copilot Dockerfile builds without referencing non-existent files
- docs/policy/schema.md is removed
</success_criteria>

<output>
After completion, create `.planning/phases/04-observability-and-distribution/04-03-SUMMARY.md`
</output>
