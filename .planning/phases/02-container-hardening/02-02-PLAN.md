---
phase: 02-container-hardening
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: []
autonomous: false
requirements: [HARD-01, HARD-02, HARD-03, HARD-04]

must_haves:
  truths:
    - "Sandbox container starts successfully under read_only: true without exiting"
    - "bash tests/smoke-hardening.sh exits 0 with all checks PASS"
    - "bash tests/smoke-proxy.sh exits 0 (no regressions in proxy wiring)"
    - "Writing to / fails but writing to /tmp and /run succeeds inside the live container"
  artifacts:
    - path: "tests/smoke-hardening.sh"
      provides: "Executed and green"
    - path: "docker-compose.yml"
      provides: "Running container with hardening applied"
  key_links:
    - from: "docker compose ps"
      to: "agent-sandbox"
      via: "service status shows running/healthy"
      pattern: "Up"
---

<objective>
Human verification that the hardened sandbox container starts correctly, all four HARD-* requirements pass the smoke test, and no proxy regressions were introduced.

Purpose: Confirm the hardening changes work in a live running container before closing the phase.
Output: No files modified — this plan gates phase completion on verified behavior.
</objective>

<execution_context>
@/home/jani/.claude/get-shit-done/workflows/execute-plan.md
@/home/jani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-container-hardening/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and start the hardened sandbox</name>
  <files></files>
  <read_first>
    - .planning/phases/02-container-hardening/02-01-SUMMARY.md — confirm Plan 01 completed successfully
  </read_first>
  <action>
Run the following commands in sequence. Stop and report if any command fails.

1. Build the base image to pick up entrypoint.sh changes:
   python3 images/build.py all

2. Bring down any running containers cleanly:
   docker compose down

3. Start proxy and sandbox:
   docker compose up -d

4. Wait for health check (proxy must be healthy before sandbox starts):
   docker compose ps

5. Confirm agent-sandbox is running (not exited):
   docker compose ps agent-sandbox

If agent-sandbox shows "Exit" or "Exited" status, capture logs immediately:
   docker logs agent-sandbox

Report the container status output before proceeding.
  </action>
  <verify>
    <automated>docker compose ps agent-sandbox | grep -E "Up|running"</automated>
  </verify>
  <acceptance_criteria>
    - python3 images/build.py all exits 0
    - docker compose up -d exits 0
    - docker compose ps agent-sandbox shows the container in running/Up state (not exited)
    - No "Read-only file system" errors in docker logs agent-sandbox
  </acceptance_criteria>
  <done>Hardened sandbox container is running after build + start cycle</done>
</task>

<task type="auto">
  <name>Task 2: Run full smoke test suite</name>
  <files></files>
  <read_first>
    - tests/smoke-hardening.sh — review checks before running
    - tests/smoke-proxy.sh — review proxy checks
  </read_first>
  <action>
Run both smoke test scripts and collect results:

1. Hardening smoke test (covers HARD-01 through HARD-04):
   bash tests/smoke-hardening.sh

2. Proxy smoke test (regression check — must still pass after hardening changes):
   bash tests/smoke-proxy.sh

Capture full output from both runs.

If any checks FAIL:
- For HARD-01 rootfs write failure: check if /tmp tmpfs entries are present in docker-compose.yml
- For HARD-02 capability checks: confirm cap_drop: [ALL] is in docker-compose.yml
- For HARD-04 resource limit checks: confirm deploy.resources.limits is present; confirm using `docker compose` not `docker-compose`
- For proxy regressions: check that proxy service keys in docker-compose.yml are unchanged
  </action>
  <verify>
    <automated>bash tests/smoke-hardening.sh && bash tests/smoke-proxy.sh</automated>
  </verify>
  <acceptance_criteria>
    - bash tests/smoke-hardening.sh exits 0 with "0 failed" in output
    - bash tests/smoke-proxy.sh exits 0 with "0 failed" in output
    - HARD-01: "ReadonlyRootfs: true" check PASSES
    - HARD-01: "/tmp is writable" check PASSES
    - HARD-01: "Rootfs is read-only (write to / fails)" check PASSES
    - HARD-02: "No capabilities added (CapAdd is null)" check PASSES
    - HARD-02: "CapDrop contains ALL" check PASSES
    - HARD-02: "no-new-privileges is set" check PASSES
    - HARD-03: "Default seccomp active (not unconfined)" check PASSES
    - HARD-04: Memory, CPU, PID limit checks all PASS
  </acceptance_criteria>
  <done>Both smoke test scripts exit 0 with all checks PASS — zero failures</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — sandbox hardening confirmed</name>
  <files></files>
  <read_first>
    - Output from Task 1 (container status)
    - Output from Task 2 (smoke test results)
  </read_first>
  <action>
Present the smoke test results from Task 2 to the user. Ask the user to run these two manual checks:

1. Workspace writes still work:
   docker compose exec agent-sandbox bash -c "touch /workspace/.test-write && rm /workspace/.test-write && echo workspace-writable"
   Expected output: "workspace-writable"

2. Rootfs is read-protected:
   docker compose exec agent-sandbox bash -c "touch /test-read-only-check 2>&1 || true"
   Expected output: contains "Read-only file system"

Optionally, offer to test env-var override of resource limits:
   SANDBOX_MEMORY=2g SANDBOX_CPUS=1 docker compose up -d
   docker inspect agent-sandbox | python3 -c "import json,sys; c=json.load(sys.stdin)[0]; print('Memory:', c['HostConfig']['Memory'], 'NanoCpus:', c['HostConfig']['NanoCpus'])"
   Expected: Memory ~2147483648, NanoCpus ~1000000000
   Then restore: docker compose down && docker compose up -d
  </action>
  <verify>
    <automated>MISSING — this task requires human confirmation of live container behavior</automated>
  </verify>
  <acceptance_criteria>
    - User confirms "workspace-writable" output from workspace write test
    - User confirms "Read-only file system" error from rootfs write attempt
    - User types "approved" to signal phase completion
  </acceptance_criteria>
  <done>User has approved: both smoke tests green, workspace is writable, rootfs is protected, Phase 2 requirements HARD-01 through HARD-04 verified</done>
</task>

</tasks>

<verification>
Phase 2 is complete when:
1. bash tests/smoke-hardening.sh exits 0 (all HARD-* checks PASS)
2. bash tests/smoke-proxy.sh exits 0 (no regressions)
3. Human confirmed workspace is writable and rootfs is protected
</verification>

<success_criteria>
- All 11 checks in smoke-hardening.sh pass (0 failed)
- All proxy checks in smoke-proxy.sh pass (0 failed)
- Human approves the checkpoint
- Phase 2 requirements HARD-01, HARD-02, HARD-03, HARD-04 are all verified
</success_criteria>

<output>
After completion, create `.planning/phases/02-container-hardening/02-02-SUMMARY.md`
</output>
