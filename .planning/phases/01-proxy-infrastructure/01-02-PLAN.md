---
phase: 01-proxy-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - images/base/Dockerfile
  - images/base/entrypoint.sh
autonomous: true
requirements: [MIG-01, MIG-02]

must_haves:
  truths:
    - "images/base/Dockerfile no longer references iptables, ipset, or aggregate packages"
    - "images/base/Dockerfile no longer COPYs policy.json or init-firewall.py"
    - "images/base/entrypoint.sh no longer contains the firewall initialization block"
    - "entrypoint.sh starts directly with cd /workspace after the set -e header"
  artifacts:
    - path: "images/base/Dockerfile"
      provides: "Cleaned base image without firewall tooling"
      contains: "FROM debian:bookworm"
    - path: "images/base/entrypoint.sh"
      provides: "Cleaned entrypoint without firewall init"
      contains: "cd /workspace"
  key_links:
    - from: "images/base/entrypoint.sh"
      to: "gosu drop-privileges call"
      via: "exec gosu after UID/GID adjustment"
      pattern: "exec gosu"
---

<objective>
Remove the iptables/ipset firewall approach from the base image: clean Dockerfile of firewall packages and COPY directives, clean entrypoint.sh of the firewall initialization block.

Purpose: The clean cutover — this change and Plan 01 (proxy image) together constitute the full migration. After this plan, the base image no longer needs NET_ADMIN or NET_RAW. Docker Compose wiring (removing cap_add) is Plan 03.

Output: Cleaned images/base/Dockerfile and images/base/entrypoint.sh. The init-firewall.py and policy.json files are still present on disk; their physical deletion happens in Plan 04 after smoke tests pass.
</objective>

<execution_context>
@/home/jani/.claude/get-shit-done/workflows/execute-plan.md
@/home/jani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-proxy-infrastructure/01-CONTEXT.md
@.planning/phases/01-proxy-infrastructure/01-RESEARCH.md

<interfaces>
<!-- Current state of files being modified — read before changing anything -->

images/base/Dockerfile current firewall-related content to REMOVE:
  Line 31:  iptables \
  Line 32:  ipset \
  Line 35:  aggregate \
  Lines 94-101: (after zsh-in-docker install block)
    # Copy default network policy and firewall script
    COPY policy.json /etc/agent-sandbox/policy.json
    COPY init-firewall.py /usr/local/bin/
    USER root
    RUN chmod 644 /etc/agent-sandbox/policy.json && \
      chmod +x /usr/local/bin/init-firewall.py

  KEEP: iproute2 (line 33) — useful for debugging network routes
  KEEP: dnsutils (line 34) — provides dig/getent, needed to verify proxy DNS resolution

images/base/entrypoint.sh current firewall block to REMOVE (lines 10-25):
  # Initialize firewall if not already done
  # Check if allowed-domains ipset exists (created by init-firewall.py)
  if ! ipset list allowed-domains >/dev/null 2>&1; then
      echo "Initializing firewall..."
      if ! /usr/local/bin/init-firewall.py; then
          echo ""
          echo "=========================================="
          echo "FATAL: Firewall initialization failed!"
          echo "Container cannot start without working firewall."
          echo "Check the errors above and rebuild the image."
          echo "=========================================="
          exit 1
    fi
  else
    echo "Firewall already initialized."
  fi

  After removal, entrypoint.sh should proceed directly to: cd /workspace
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean firewall packages and COPY directives from base Dockerfile</name>
  <files>images/base/Dockerfile</files>
  <read_first>
    - images/base/Dockerfile (read the full file before making changes — understand exact line positions)
    - .planning/phases/01-proxy-infrastructure/01-RESEARCH.md (section "Base Dockerfile Lines to Remove", section "State of the Art" table)
  </read_first>
  <action>
Edit images/base/Dockerfile to remove exactly these items:

1. In the apt-get install block, remove these three package lines:
     iptables \
     ipset \
     aggregate \
   KEEP iproute2 and dnsutils — they are useful for debugging and are NOT firewall-specific.
   The apt-get install block should still be valid bash after removal (no trailing backslash on the last kept line before &&).

2. Remove the entire "Copy default network policy and firewall script" block, which is:
     # Copy default network policy and firewall script
     COPY policy.json /etc/agent-sandbox/policy.json
     COPY init-firewall.py /usr/local/bin/
     USER root
     RUN chmod 644 /etc/agent-sandbox/policy.json && \
       chmod +x /usr/local/bin/init-firewall.py
   This block appears after the zsh-in-docker install RUN step. Remove it entirely.

3. Do NOT remove or alter any other section of the Dockerfile. The git, curl, gosu, zsh, fzf, neovim, build-essential, iproute2, dnsutils, and all other packages remain.

4. Verify the Dockerfile remains syntactically valid after edits: the apt-get install block must end with && apt-get clean, and the USER/RUN sequences before and after the removed block must be intact.

The files init-firewall.py and policy.json remain on disk for now. Physical deletion happens in Plan 04 after smoke tests pass.
  </action>
  <verify>
    <automated>grep -v "iptables\|ipset\|aggregate\|init-firewall\|policy\.json" /home/jani/devel/agent-sandbox/images/base/Dockerfile | grep -q "iproute2" && ! grep -q "iptables" /home/jani/devel/agent-sandbox/images/base/Dockerfile && ! grep -q "ipset" /home/jani/devel/agent-sandbox/images/base/Dockerfile && ! grep -q "aggregate" /home/jani/devel/agent-sandbox/images/base/Dockerfile && ! grep -q "init-firewall" /home/jani/devel/agent-sandbox/images/base/Dockerfile && ! grep -q "COPY policy.json" /home/jani/devel/agent-sandbox/images/base/Dockerfile && grep -q "iproute2" /home/jani/devel/agent-sandbox/images/base/Dockerfile && grep -q "dnsutils" /home/jani/devel/agent-sandbox/images/base/Dockerfile && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - images/base/Dockerfile does NOT contain "iptables" (grep returns empty)
    - images/base/Dockerfile does NOT contain "ipset" (grep returns empty)
    - images/base/Dockerfile does NOT contain "aggregate" (grep returns empty)
    - images/base/Dockerfile does NOT contain "init-firewall" (grep returns empty)
    - images/base/Dockerfile does NOT contain "COPY policy.json" (grep returns empty)
    - images/base/Dockerfile DOES contain "iproute2" (kept for debugging)
    - images/base/Dockerfile DOES contain "dnsutils" (kept for debugging, needed for getent)
    - images/base/Dockerfile DOES contain "FROM debian:bookworm" (base image unchanged)
    - images/base/Dockerfile DOES contain "gosu" in apt-get install block
    - images/base/Dockerfile DOES contain "ENTRYPOINT" directive
  </acceptance_criteria>
  <done>Base Dockerfile cleaned of all firewall tooling. Image can be rebuilt without iptables, ipset, or aggregate. No firewall scripts are COPYed into the image.</done>
</task>

<task type="auto">
  <name>Task 2: Remove firewall initialization block from entrypoint.sh</name>
  <files>images/base/entrypoint.sh</files>
  <read_first>
    - images/base/entrypoint.sh (read the full file before editing — understand exact lines to remove)
    - .planning/phases/01-proxy-infrastructure/01-RESEARCH.md (section "Entrypoint Cleanup (lines to remove from entrypoint.sh)")
  </read_first>
  <action>
Edit images/base/entrypoint.sh to remove the firewall initialization block. The block to remove is lines 10-25 in the current file:

    # Initialize firewall if not already done
    # Check if allowed-domains ipset exists (created by init-firewall.py)
    if ! ipset list allowed-domains >/dev/null 2>&1; then
        echo "Initializing firewall..."
        if ! /usr/local/bin/init-firewall.py; then
            echo ""
            echo "=========================================="
            echo "FATAL: Firewall initialization failed!"
            echo "Container cannot start without working firewall."
            echo "Check the errors above and rebuild the image."
            echo "=========================================="
            exit 1
      fi
    else
      echo "Firewall already initialized."
    fi

After removal, the file structure should be:
    #!/bin/bash
    set -e
    # set -ex for debugging

    # entrypoint.sh must be run as root...
    # This script is both the container's entrypoint, but also symlinked as the `dev-shell` handle
    # so this script must take that into account...

    cd /workspace

    # the host bind mount is
    TARGET_DIR=.
    [... rest of UID/GID adjustment and gosu exec unchanged ...]

Do NOT alter the UID/GID adjustment logic, the gosu exec line, or any comments about the script's dual purpose. Only the firewall block (the if/else around ipset and init-firewall.py) is removed.
  </action>
  <verify>
    <automated>! grep -q "ipset" /home/jani/devel/agent-sandbox/images/base/entrypoint.sh && ! grep -q "init-firewall" /home/jani/devel/agent-sandbox/images/base/entrypoint.sh && ! grep -q "Firewall" /home/jani/devel/agent-sandbox/images/base/entrypoint.sh && grep -q "exec gosu" /home/jani/devel/agent-sandbox/images/base/entrypoint.sh && grep -q "cd /workspace" /home/jani/devel/agent-sandbox/images/base/entrypoint.sh && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - images/base/entrypoint.sh does NOT contain "ipset" (grep returns empty)
    - images/base/entrypoint.sh does NOT contain "init-firewall" (grep returns empty)
    - images/base/entrypoint.sh does NOT contain "Firewall" (grep returns empty, case-sensitive)
    - images/base/entrypoint.sh does NOT contain "allowed-domains" (grep returns empty)
    - images/base/entrypoint.sh DOES contain "exec gosu" (privilege drop preserved)
    - images/base/entrypoint.sh DOES contain "cd /workspace" (workspace navigation preserved)
    - images/base/entrypoint.sh DOES contain "set -e" (error handling preserved)
    - images/base/entrypoint.sh first executable statement after set -e comments is "cd /workspace"
  </acceptance_criteria>
  <done>entrypoint.sh no longer initializes iptables or calls init-firewall.py. Container startup proceeds directly to workspace and UID/GID adjustment.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -E "iptables|ipset|aggregate|init-firewall" images/base/Dockerfile` — no output
2. `grep -E "ipset|init-firewall|Firewall|allowed-domains" images/base/entrypoint.sh` — no output
3. `grep "iproute2\|dnsutils" images/base/Dockerfile` — both present
4. `grep "exec gosu\|cd /workspace" images/base/entrypoint.sh` — both present
</verification>

<success_criteria>
- images/base/Dockerfile contains no references to iptables, ipset, aggregate, init-firewall.py, or policy.json COPY
- images/base/entrypoint.sh contains no firewall initialization block
- iproute2 and dnsutils are retained in Dockerfile (debug value)
- gosu privilege drop and UID/GID adjustment logic are intact in entrypoint.sh
</success_criteria>

<output>
After completion, create .planning/phases/01-proxy-infrastructure/01-02-SUMMARY.md
</output>
