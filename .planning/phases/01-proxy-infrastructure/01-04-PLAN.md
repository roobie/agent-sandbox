---
phase: 01-proxy-infrastructure
plan: 04
type: execute
wave: 3
depends_on: [01-03]
files_modified:
  - tests/smoke-proxy.sh
  - images/base/init-firewall.py
  - images/base/policy.json
  - images/agents/claude/policy.json
  - images/agents/copilot/policy.json
autonomous: false
requirements: [PROXY-01, PROXY-02, PROXY-03, PROXY-04, PROXY-05, PROXY-06, PROXY-07, MIG-01, MIG-02]

must_haves:
  truths:
    - "tests/smoke-proxy.sh runs and verifies all PROXY-* and MIG-* requirements"
    - "init-firewall.py is absent from images/base/ — completely deleted"
    - "images/base/policy.json is absent — completely deleted"
    - "images/agents/claude/policy.json is absent — completely deleted"
    - "images/agents/copilot/policy.json is absent — completely deleted"
    - "smoke test confirms sandbox cannot reach internet directly (PROXY-05)"
    - "smoke test confirms proxy access log shows sandbox traffic (PROXY-04)"
  artifacts:
    - path: "tests/smoke-proxy.sh"
      provides: "Automated verification of all Phase 1 requirements"
      contains: "PROXY-01"
  key_links:
    - from: "tests/smoke-proxy.sh"
      to: "docker compose"
      via: "docker compose exec and docker inspect commands"
      pattern: "docker compose exec agent-sandbox"
---

<objective>
Create the smoke test suite, run it to verify the complete proxy infrastructure works, then delete the old firewall files that are now superseded.

Purpose: Verification-gated cleanup. The smoke test proves the proxy system works end-to-end before we delete the files that the old approach depended on. Files are only deleted after the smoke test passes.

Output: tests/smoke-proxy.sh covering all PROXY-* and MIG-* requirements; init-firewall.py and all policy.json files deleted.
</objective>

<execution_context>
@/home/jani/.claude/get-shit-done/workflows/execute-plan.md
@/home/jani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-proxy-infrastructure/01-CONTEXT.md
@.planning/phases/01-proxy-infrastructure/01-RESEARCH.md
@.planning/phases/01-proxy-infrastructure/01-VALIDATION.md

<interfaces>
<!-- Test requirements from RESEARCH.md Validation Architecture section -->

PROXY-01: docker compose ps proxy | grep Up
PROXY-02: docker compose exec agent-sandbox curl -sf -x http://proxy:3128 https://api.anthropic.com (allowed domain passes)
          docker compose exec agent-sandbox curl -sf -x http://proxy:3128 https://notallowed.example.com (blocked domain fails)
PROXY-03: Same as PROXY-02 — SNI verification is what makes the allowed/blocked distinction work
PROXY-04: docker logs agent-sandbox-proxy 2>&1 | grep CONNECT (after two sandbox requests)
PROXY-05: docker compose exec agent-sandbox curl --noproxy '*' -sf --connect-timeout 3 https://api.anthropic.com (must fail — no direct internet)
PROXY-06: docker compose exec agent-sandbox env | grep -i proxy (HTTP_PROXY and HTTPS_PROXY present)
PROXY-07: docker compose stop proxy && docker compose up -d agent-sandbox (sandbox stays waiting, not error)
MIG-01:   docker compose exec agent-sandbox test ! -f /usr/local/bin/init-firewall.py
MIG-02:   docker inspect agent-sandbox | python3 -c "import json,sys; c=json.load(sys.stdin); print(c[0]['HostConfig']['CapAdd'])" (must be null)

Critical anti-pattern to avoid: Never run docker compose up while init-firewall.py is still in the image.
Running both simultaneously corrupts Docker DNS NAT. Migration must be a clean cutover.
Verify migration is clean with: docker compose exec agent-sandbox getent hosts proxy (must resolve)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create smoke test suite</name>
  <files>tests/smoke-proxy.sh</files>
  <read_first>
    - .planning/phases/01-proxy-infrastructure/01-RESEARCH.md (section "Phase Requirements to Test Map", section "Validation Architecture", section "Pitfall 3: DNS Resolution Broken After Migration")
    - .planning/phases/01-proxy-infrastructure/01-VALIDATION.md (test approach and coverage map)
    - docker-compose.yml (service names, network names — use exact container_name values)
  </read_first>
  <action>
Create tests/ directory and write tests/smoke-proxy.sh. The script must:

1. Be executable (set in the script or note chmod +x tests/smoke-proxy.sh after creation)
2. Use set -euo pipefail
3. Print PASS/FAIL for each requirement check with the requirement ID
4. Exit 0 only if all checks pass; exit 1 on first failure (or collect failures and exit 1 at end)

Write tests/smoke-proxy.sh with this structure:

    #!/usr/bin/env bash
    set -euo pipefail

    PASS=0
    FAIL=0

    check() {
        local id="$1"
        local desc="$2"
        shift 2
        if "$@" >/dev/null 2>&1; then
            echo "PASS [$id] $desc"
            ((PASS++))
        else
            echo "FAIL [$id] $desc"
            ((FAIL++))
        fi
    }

    check_fail() {
        local id="$1"
        local desc="$2"
        shift 2
        if ! "$@" >/dev/null 2>&1; then
            echo "PASS [$id] $desc (expected failure confirmed)"
            ((PASS++))
        else
            echo "FAIL [$id] $desc (should have failed but succeeded)"
            ((FAIL++))
        fi
    }

    echo "=== Agent Sandbox Proxy Smoke Tests ==="
    echo ""

    # PROXY-01: Proxy container is running
    check PROXY-01 "Proxy container is running" \
        bash -c "docker compose ps proxy | grep -q 'Up\|running'"

    # PROXY-05: Sandbox has no direct internet (must fail without proxy)
    check_fail PROXY-05 "Sandbox cannot reach internet directly (no proxy)" \
        docker compose exec -T agent-sandbox \
            curl --noproxy '*' -sf --connect-timeout 3 https://api.anthropic.com

    # PROXY-06: Proxy env vars are set in sandbox
    check PROXY-06 "HTTP_PROXY env var is set in sandbox" \
        bash -c "docker compose exec -T agent-sandbox env | grep -q 'HTTP_PROXY=http://proxy:3128'"

    check PROXY-06 "HTTPS_PROXY env var is set in sandbox" \
        bash -c "docker compose exec -T agent-sandbox env | grep -q 'HTTPS_PROXY=http://proxy:3128'"

    # PROXY-02 + PROXY-03: Allowlisted domain passes, blocked domain fails
    check PROXY-02 "Allowed domain reaches proxy (api.anthropic.com)" \
        docker compose exec -T agent-sandbox \
            curl -sf --max-time 10 -x http://proxy:3128 https://api.anthropic.com

    check_fail PROXY-02 "Blocked domain denied by proxy (notallowed.example.com)" \
        docker compose exec -T agent-sandbox \
            curl -sf --max-time 5 -x http://proxy:3128 https://notallowed.example.com

    check PROXY-03 "SNI verification: proxy DNS resolves inside sandbox" \
        bash -c "docker compose exec -T agent-sandbox getent hosts proxy | grep -q proxy"

    # PROXY-04: Multi-sandbox traffic appears in proxy access log
    # Make a second request from sandbox to verify access log has entries
    docker compose exec -T agent-sandbox curl -sf --max-time 10 -x http://proxy:3128 https://api.anthropic.com >/dev/null 2>&1 || true
    check PROXY-04 "Proxy access log shows CONNECT entries (multi-sandbox routing visible)" \
        bash -c "docker logs agent-sandbox-proxy 2>&1 | grep -q 'CONNECT'"

    # PROXY-07: Health check — proxy service is healthy
    check PROXY-07 "Proxy service health check passes" \
        bash -c "docker compose ps proxy | grep -q 'healthy'"

    # MIG-01: init-firewall.py is absent from sandbox container
    check MIG-01 "init-firewall.py is absent from sandbox container" \
        bash -c "docker compose exec -T agent-sandbox test ! -f /usr/local/bin/init-firewall.py"

    # MIG-02: No NET_ADMIN or NET_RAW capabilities on sandbox container
    check MIG-02 "Sandbox container has no NET_ADMIN capability" \
        bash -c "docker inspect agent-sandbox | python3 -c \"import json,sys; c=json.load(sys.stdin); caps=c[0]['HostConfig']['CapAdd']; exit(0 if caps is None or 'NET_ADMIN' not in caps else 1)\""

    check MIG-02 "Sandbox container has no NET_RAW capability" \
        bash -c "docker inspect agent-sandbox | python3 -c \"import json,sys; c=json.load(sys.stdin); caps=c[0]['HostConfig']['CapAdd']; exit(0 if caps is None or 'NET_RAW' not in caps else 1)\""

    echo ""
    echo "=== Results: $PASS passed, $FAIL failed ==="

    if [ "$FAIL" -gt 0 ]; then
        exit 1
    fi

After writing the file, run: chmod +x tests/smoke-proxy.sh

Note: The test for api.anthropic.com in PROXY-02 may fail if the domain requires valid auth headers. If so, substitute with a different allowlisted domain that returns a valid HTTP response: .github.com (e.g., https://github.com) or .mise-versions.jdx.dev. The intent is to verify proxy routing, not API authentication.
  </action>
  <verify>
    <automated>test -f /home/jani/devel/agent-sandbox/tests/smoke-proxy.sh && test -x /home/jani/devel/agent-sandbox/tests/smoke-proxy.sh && grep -q "PROXY-01" /home/jani/devel/agent-sandbox/tests/smoke-proxy.sh && grep -q "PROXY-05" /home/jani/devel/agent-sandbox/tests/smoke-proxy.sh && grep -q "MIG-01" /home/jani/devel/agent-sandbox/tests/smoke-proxy.sh && grep -q "MIG-02" /home/jani/devel/agent-sandbox/tests/smoke-proxy.sh && bash -n /home/jani/devel/agent-sandbox/tests/smoke-proxy.sh && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - tests/smoke-proxy.sh exists and is executable (test -x passes)
    - tests/smoke-proxy.sh is valid bash (bash -n exits 0)
    - tests/smoke-proxy.sh contains "PROXY-01" check
    - tests/smoke-proxy.sh contains "PROXY-02" check
    - tests/smoke-proxy.sh contains "PROXY-03" check
    - tests/smoke-proxy.sh contains "PROXY-04" check
    - tests/smoke-proxy.sh contains "PROXY-05" check (expected-failure check)
    - tests/smoke-proxy.sh contains "PROXY-06" check
    - tests/smoke-proxy.sh contains "PROXY-07" check
    - tests/smoke-proxy.sh contains "MIG-01" check
    - tests/smoke-proxy.sh contains "MIG-02" check
    - tests/smoke-proxy.sh contains "docker inspect agent-sandbox" for cap check
    - tests/smoke-proxy.sh contains "getent hosts proxy" for DNS check
  </acceptance_criteria>
  <done>Smoke test suite created covering all 9 Phase 1 requirements. Script is executable and bash-syntax valid.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete proxy infrastructure end-to-end</name>
  <what-built>
    Complete proxy infrastructure:
    - Plan 01: images/proxy/ (Dockerfile, squid.conf, allowlist.txt) + config/allowlist.txt
    - Plan 02: images/base/Dockerfile and entrypoint.sh cleaned of firewall code
    - Plan 03: docker-compose.yml with proxy service + dual networks + no cap_add; images/build.py with build_proxy
    - Plan 04 Task 1: tests/smoke-proxy.sh

    To run the full system: first build images, then start, then run smoke tests.
  </what-built>
  <how-to-verify>
    Run these commands in order:

    1. Build the proxy image:
       python3 images/build.py proxy

    2. Build the base and claude images (they no longer contain firewall code):
       python3 images/build.py base
       python3 images/build.py claude

    3. Start the proxy and sandbox:
       docker compose up -d

    4. Wait for proxy to be healthy:
       docker compose ps
       (look for "healthy" next to proxy service — may take 15-30s)

    5. Verify proxy DNS resolves inside sandbox:
       docker compose exec agent-sandbox getent hosts proxy
       (should print an IP address line with "proxy")

    6. Run the smoke tests:
       bash tests/smoke-proxy.sh

    Expected result: All checks print PASS. Results line shows "0 failed".

    If any check fails, examine the failure reason and report before continuing to file deletion.
  </how-to-verify>
  <resume-signal>Type "smoke tests pass" to continue to file deletion, or describe failures to investigate</resume-signal>
  <action>Human verification step: build images, start compose stack, run smoke tests. See how-to-verify for exact commands.</action>
  <verify><automated>bash tests/smoke-proxy.sh</automated></verify>
  <done>All smoke tests pass: proxy running, allowed domains work, blocked domains fail, sandbox has no direct internet, no NET_ADMIN capability.</done>
</task>

<task type="auto">
  <name>Task 3: Delete superseded firewall files</name>
  <files>images/base/init-firewall.py, images/base/policy.json, images/agents/claude/policy.json, images/agents/copilot/policy.json</files>
  <read_first>
    - images/base/init-firewall.py (confirm the file exists — it should, we only removed it from the Dockerfile in Plan 02)
    - images/base/policy.json (confirm existence)
    - images/agents/claude/policy.json (confirm existence — domains already migrated to config/allowlist.txt in Plan 01)
    - images/agents/copilot/policy.json (confirm existence)
  </read_first>
  <action>
The smoke tests passed (checkpoint cleared). Delete all four files that the old iptables approach depended on:

    rm images/base/init-firewall.py
    rm images/base/policy.json
    rm images/agents/claude/policy.json
    rm images/agents/copilot/policy.json

Rationale:
- images/base/init-firewall.py: The 435-line Python firewall script. Domain lists migrated to config/allowlist.txt. Dockerfile no longer COPYs it (Plan 02). DELETE.
- images/base/policy.json: The base policy with services: ["github"]. Superseded by allowlist.txt. DELETE.
- images/agents/claude/policy.json: Claude-specific domain list. Migrated to config/allowlist.txt. DELETE.
- images/agents/copilot/policy.json: Copilot domain list. Migrated to config/allowlist.txt. DELETE.

After deletion, verify none of these files exist. The domain data is preserved in config/allowlist.txt and images/proxy/allowlist.txt.
  </action>
  <verify>
    <automated>test ! -f /home/jani/devel/agent-sandbox/images/base/init-firewall.py && test ! -f /home/jani/devel/agent-sandbox/images/base/policy.json && test ! -f /home/jani/devel/agent-sandbox/images/agents/claude/policy.json && test ! -f /home/jani/devel/agent-sandbox/images/agents/copilot/policy.json && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - images/base/init-firewall.py does NOT exist (test ! -f passes)
    - images/base/policy.json does NOT exist (test ! -f passes)
    - images/agents/claude/policy.json does NOT exist (test ! -f passes)
    - images/agents/copilot/policy.json does NOT exist (test ! -f passes)
    - config/allowlist.txt DOES exist (domain data is preserved)
    - images/proxy/allowlist.txt DOES exist (domain data is preserved)
  </acceptance_criteria>
  <done>All superseded firewall files deleted. The old iptables approach no longer exists in the repository. Domain data is preserved in the Squid allowlist format.</done>
</task>

</tasks>

<verification>
After all tasks and checkpoint complete:
1. `bash tests/smoke-proxy.sh` — all checks pass, exit 0
2. `test ! -f images/base/init-firewall.py && test ! -f images/base/policy.json` — both absent
3. `test ! -f images/agents/claude/policy.json && test ! -f images/agents/copilot/policy.json` — both absent
4. `test -f config/allowlist.txt && test -f images/proxy/allowlist.txt` — domain data preserved
5. `docker compose exec agent-sandbox getent hosts proxy` — resolves (DNS not corrupted)
</verification>

<success_criteria>
- tests/smoke-proxy.sh exists, is executable, and all checks pass
- init-firewall.py and all policy.json files are deleted from the repository
- Smoke tests confirm: proxy running, allowed domain passes, blocked domain fails, no direct internet from sandbox, proxy env vars set, NET_ADMIN absent
- Phase 1 is complete: Squid proxy is the sole egress path, iptables approach fully removed
</success_criteria>

<output>
After completion, create .planning/phases/01-proxy-infrastructure/01-04-SUMMARY.md
</output>
