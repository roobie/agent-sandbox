---
phase: 03-development-environment
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - config/allowlist.txt
autonomous: true
requirements:
  - DEVENV-04
  - DEVENV-07
  - ORCH-04

must_haves:
  truths:
    - "Package cache volumes (npm-cache, pip-cache, go-cache) exist in docker-compose.yml and persist across sandbox stop/start"
    - "GOPROXY and CARGO_HTTP_PROXY env vars are set in the sandbox service environment"
    - "The proxy allowlist includes domains for npm, cargo, Go modules, and mise runtime downloads"
    - "Workspace bind-mount is present in compose service for /workspace"
  artifacts:
    - path: "docker-compose.yml"
      provides: "npm-cache, pip-cache, go-cache named volumes + GOPROXY + CARGO_HTTP_PROXY env vars"
      contains: "npm-cache"
    - path: "config/allowlist.txt"
      provides: "Allowlist entries for all package manager and runtime download domains"
      contains: ".npmjs.org"
  key_links:
    - from: "docker-compose.yml volumes section"
      to: "agent-sandbox service volumes"
      via: "named volume declarations matching mount paths"
      pattern: "npm-cache:/home/dev/.npm"
    - from: "config/allowlist.txt"
      to: "Squid ACL"
      via: "host-mounted allowlist override"
      pattern: ".crates.io"
---

<objective>
Add package cache volumes to docker-compose.yml, set Go proxy and Cargo proxy env vars, and expand the Squid allowlist to include all domains needed by the new package managers and runtime downloads.

Purpose: Cache volumes ensure that a second `npm install` of the same package does not re-download from the internet. The allowlist expansion ensures that npm, cargo, go get, and mise runtime installs are not blocked by Squid ACL.

Output: Updated `docker-compose.yml` with three new named volumes and two new env vars; updated `config/allowlist.txt` with package manager and runtime download domains.
</objective>

<execution_context>
@/home/jani/.claude/get-shit-done/workflows/execute-plan.md
@/home/jani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-development-environment/03-CONTEXT.md
@.planning/phases/03-development-environment/03-RESEARCH.md

<interfaces>
<!-- Current docker-compose.yml volumes and environment sections. -->

Current named volumes (docker-compose.yml bottom):
```yaml
volumes:
  claude-state:
  command-history:
  mise-state:
  cargo-state:
  local-bin-state:
```

Current agent-sandbox service volume mounts:
```yaml
    volumes:
      - .:/workspace
      - claude-state:/home/dev/.claude
      - command-history:/commandhistory
      - mise-state:/home/dev/.mise
      - cargo-state:/home/dev/.cargo
      - local-bin-state:/home/dev/.local/bin
      - type: tmpfs
        target: /tmp
        tmpfs:
          size: 268435456
      - type: tmpfs
        target: /run
        tmpfs:
          size: 67108864
```

Current agent-sandbox environment section ends with:
```yaml
      - NO_PROXY=localhost,127.0.0.1
      - no_proxy=localhost,127.0.0.1
```

Current config/allowlist.txt:
```
.github.com
.githubusercontent.com
.api.anthropic.com
.statsig.anthropic.com
.statsig.com
.sentry.io
.pypi.org
.files.pythonhosted.org
.mise-versions.jdx.dev
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add package cache volumes and proxy env vars to docker-compose.yml</name>
  <files>docker-compose.yml</files>
  <read_first>
    - docker-compose.yml (full file — understand exact indentation and current volume list before editing)
    - .planning/phases/03-development-environment/03-RESEARCH.md (lines 333-362 — cache volume paths and compose additions)
    - .planning/phases/03-development-environment/03-CONTEXT.md (lines 53-58 — volume names and mount paths)
  </read_first>
  <action>
Make two additions to docker-compose.yml:

**1. Add volume mounts to the agent-sandbox service** (after `- local-bin-state:/home/dev/.local/bin` and before the tmpfs entries):

```yaml
      - npm-cache:/home/dev/.npm
      - pip-cache:/home/dev/.cache/pip
      - go-cache:/home/dev/go/pkg/mod
```

**2. Add env vars to the agent-sandbox environment section** (after `- no_proxy=localhost,127.0.0.1`):

```yaml
      # Go module proxy — controls where go get fetches modules from
      - GOPROXY=https://proxy.golang.org,direct
      # Cargo proxy fallback env var — bypasses volume-shadowed config.toml
      - CARGO_HTTP_PROXY=http://proxy:3128
```

**3. Add named volume declarations** to the top-level `volumes:` section (after `local-bin-state:`):

```yaml
  npm-cache:
  pip-cache:
  go-cache:
```

Note: The workspace bind-mount `- .:/workspace` is already present in the compose file (ORCH-04 satisfied). Do not remove or modify it.

Note: Indentation in docker-compose.yml uses 2 spaces. Match existing indentation exactly.
  </action>
  <verify>
    <automated>grep -c "npm-cache\|pip-cache\|go-cache" docker-compose.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep "npm-cache:/home/dev/.npm" docker-compose.yml` exits 0
    - `grep "pip-cache:/home/dev/.cache/pip" docker-compose.yml` exits 0
    - `grep "go-cache:/home/dev/go/pkg/mod" docker-compose.yml` exits 0
    - `grep "GOPROXY=https://proxy.golang.org,direct" docker-compose.yml` exits 0
    - `grep "CARGO_HTTP_PROXY=http://proxy:3128" docker-compose.yml` exits 0
    - `grep -c "npm-cache" docker-compose.yml` returns 2 (one mount, one declaration)
    - `grep -c "pip-cache" docker-compose.yml` returns 2
    - `grep -c "go-cache" docker-compose.yml` returns 2
    - `docker compose config 2>&1 | grep -i error` returns empty (valid YAML)
  </acceptance_criteria>
  <done>docker-compose.yml has three new cache volume mounts, three new named volume declarations, and GOPROXY + CARGO_HTTP_PROXY env vars</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Expand Squid allowlist with package manager and runtime download domains</name>
  <files>config/allowlist.txt</files>
  <read_first>
    - config/allowlist.txt (full file — see current entries before appending)
    - .planning/phases/03-development-environment/03-RESEARCH.md (lines 416-439 — exact domains to add with comments)
  </read_first>
  <action>
Append the following sections to config/allowlist.txt. Preserve the existing entries unchanged. Use the same leading-dot dstdomain syntax as existing entries (per Phase 1 decision: leading-dot ensures subdomain wildcard coverage).

```
# npm registry
.npmjs.org
.npmjs.com

# cargo / crates.io
.crates.io
.static.crates.io
.index.crates.io

# Go modules
.proxy.golang.org
.sum.golang.org
.storage.googleapis.com

# mise runtime downloads
.nodejs.org
.static.rust-lang.org
.bun.sh
.dl.google.com

# Python package index (uv / pip)
# Note: .pypi.org and .files.pythonhosted.org are already present above
```

Do not add the Python comment block if .pypi.org is already in the file — just add the four new sections above it (npm, cargo, go, mise).
  </action>
  <verify>
    <automated>grep -c "^\.npmjs\|^\.crates\|^\.proxy.golang\|^\.nodejs\|^\.bun" config/allowlist.txt</automated>
  </verify>
  <acceptance_criteria>
    - `grep "^\.npmjs.org" config/allowlist.txt` exits 0
    - `grep "^\.crates.io" config/allowlist.txt` exits 0
    - `grep "^\.static.crates.io" config/allowlist.txt` exits 0
    - `grep "^\.proxy.golang.org" config/allowlist.txt` exits 0
    - `grep "^\.sum.golang.org" config/allowlist.txt` exits 0
    - `grep "^\.nodejs.org" config/allowlist.txt` exits 0
    - `grep "^\.static.rust-lang.org" config/allowlist.txt` exits 0
    - `grep "^\.bun.sh" config/allowlist.txt` exits 0
    - `grep "^\.dl.google.com" config/allowlist.txt` exits 0
    - Existing entries (.github.com, .pypi.org, etc.) still present: `grep "^\.github.com" config/allowlist.txt` exits 0
    - Total line count increased by ~14 entries: `wc -l config/allowlist.txt` shows 30+ lines
  </acceptance_criteria>
  <done>config/allowlist.txt contains all domains required for npm, cargo, go modules, and mise runtime downloads to traverse the Squid proxy successfully</done>
</task>

</tasks>

<verification>
```bash
# Validate compose YAML is syntactically correct
docker compose config > /dev/null && echo "YAML valid"

# Confirm all three cache volume types present
grep -E "npm-cache|pip-cache|go-cache" docker-compose.yml

# Confirm proxy env vars
grep -E "GOPROXY|CARGO_HTTP_PROXY" docker-compose.yml

# Confirm new allowlist domains
grep -E "npmjs|crates\.io|golang\.org|nodejs\.org|rust-lang|bun\.sh" config/allowlist.txt
```
</verification>

<success_criteria>
- `docker compose config > /dev/null` exits 0 (valid YAML)
- All three cache volume names appear twice in docker-compose.yml (mount + declaration)
- GOPROXY and CARGO_HTTP_PROXY env vars present in agent-sandbox service
- 9 new domain entries in config/allowlist.txt (npmjs.org, npmjs.com, crates.io x3, golang.org x2, nodejs.org, rust-lang.org, bun.sh, dl.google.com)
</success_criteria>

<output>
After completion, create `.planning/phases/03-development-environment/03-02-SUMMARY.md`
</output>
