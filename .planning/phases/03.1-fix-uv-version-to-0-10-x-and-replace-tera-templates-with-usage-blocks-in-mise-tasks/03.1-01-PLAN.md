---
phase: 03.1-fix-uv-version-to-0-10-x-and-replace-tera-templates-with-usage-blocks-in-mise-tasks
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - images/build.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "UV_VERSION default in build.py is 0.10.12 (a 0.10.x release)"
    - "mise.toml contains no Tera template syntax (no {{ }} blocks)"
    - "The Dockerfile still accepts UV_VERSION as a build arg and passes it to mise install"
  artifacts:
    - path: "images/build.py"
      provides: "Updated UV_VERSION default"
      contains: "0.10.12"
  key_links:
    - from: "images/build.py"
      to: "images/agents/claude/Dockerfile"
      via: "UV_VERSION build arg passed to docker build --build-arg"
      pattern: "UV_VERSION"
---

<objective>
Update the default uv version from 0.9.26 to 0.10.12 in images/build.py, and confirm mise.toml already uses usage blocks (no Tera templates remain).

Purpose: uv 0.9.26 is incompatible with current tooling expectations. 0.10.x is the stable target series; 0.10.12 is the latest release. mise.toml was already corrected by the Phase 3 executor — this plan confirms no further changes are needed there.

Output: images/build.py with UV_VERSION = "0.10.12"; confirmation that mise.toml is clean.
</objective>

<execution_context>
@/home/jani/.claude/get-shit-done/workflows/execute-plan.md
@/home/jani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@images/build.py
@images/agents/claude/Dockerfile
@mise.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump UV_VERSION default to 0.10.12 in build.py</name>
  <files>images/build.py</files>
  <read_first>images/build.py — read line 13 to confirm current value is "0.9.26" before editing</read_first>
  <action>
    In images/build.py, change line 13 from:

      UV_VERSION = os.environ.get("UV_VERSION", "0.9.26")

    to:

      UV_VERSION = os.environ.get("UV_VERSION", "0.10.12")

    No other changes. The Dockerfile already accepts UV_VERSION as a build arg (ARG UV_VERSION on line 50) and passes it to both `mise install uv@$UV_VERSION` and `mise use -g ... uv@$UV_VERSION` — those lines are correct as-is.
  </action>
  <verify>
    <automated>grep 'UV_VERSION.*0\.10\.12' /home/jani/devel/agent-sandbox/images/build.py</automated>
  </verify>
  <acceptance_criteria>Line 13 of images/build.py reads exactly: UV_VERSION = os.environ.get("UV_VERSION", "0.10.12")</acceptance_criteria>
  <done>images/build.py contains UV_VERSION default of "0.10.12"; grep returns a match</done>
</task>

<task type="auto">
  <name>Task 2: Verify mise.toml uses usage blocks (no Tera templates)</name>
  <files></files>
  <read_first>mise.toml — scan for any {{ }} Tera template syntax; expected: none found</read_first>
  <action>
    Verify that mise.toml contains no Tera template syntax by running:

      grep -n '{{' mise.toml

    Expected result: no output (exit code 1 from grep = no matches = clean).

    Also confirm the two usage blocks are present and well-formed:
    - sandbox:run task has a `usage = '''` block with `flag "--name"` and `flag "--workspace"` entries
    - sandbox:stop task has a `usage = '''` block with `flag "--name"` entry
    - Both tasks reference `${usage_name:-default}` and `${usage_workspace:-.}` (not Tera `{{ argv[0] }}` syntax)

    If any `{{ }}` patterns are found, replace them with the usage block equivalents as described above.
    Based on pre-planning inspection, no changes are expected.
  </action>
  <verify>
    <automated>grep -c '{{' /home/jani/devel/agent-sandbox/mise.toml; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>grep finds zero occurrences of "{{" in mise.toml; sandbox:run and sandbox:stop tasks use usage blocks with ${usage_*} variable references</acceptance_criteria>
  <done>mise.toml confirmed clean: no Tera templates, usage blocks present and correct</done>
</task>

</tasks>

<verification>
After both tasks:

1. Confirm version bump: `grep UV_VERSION images/build.py` shows `"0.10.12"`
2. Confirm no Tera syntax: `grep '{{' mise.toml` returns no output
3. Sanity-check Dockerfile is unmodified: `grep UV_VERSION images/agents/claude/Dockerfile` shows `ARG UV_VERSION` and two `uv@$UV_VERSION` references (unchanged)
</verification>

<success_criteria>
- images/build.py default UV_VERSION is "0.10.12"
- mise.toml contains zero Tera template `{{` occurrences
- No other files modified
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-fix-uv-version-to-0-10-x-and-replace-tera-templates-with-usage-blocks-in-mise-tasks/03.1-01-SUMMARY.md`
</output>
